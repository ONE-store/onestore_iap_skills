<!--
    ONE Store IAP — 앱 매니페스트에 추가해야 하는 선언입니다.
    아래 <uses-permission>과 <queries>를 앱 모듈의 AndroidManifest.xml에 넣으세요.
    (이미 같은 선언이 있으면 중복해서 넣지 마세요.)
-->

<!-- 결제·라이선스 검증 통신에 필요합니다. -->
<uses-permission android:name="android.permission.INTERNET" />

<!--
    Android 11+ 패키지 가시성 제한 때문에 필요합니다. 이 선언이 없으면 기기에 설치된
    원스토어 서비스를 찾지 못해 연결 자체가 실패하고, 증상이 매니페스트 문제로 보이지 않습니다.
    삭제하지 마세요.
-->
<queries>
    <intent>
        <action android:name="com.onestore.ipc.iap.IapService.ACTION" />
    </intent>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <data android:scheme="onestore" />
    </intent>
</queries>

<!--
    아래 meta-data는 <application> 태그 안에 들어갑니다. 글로벌 스토어 인앱 테스트용 옵션이며
    기본은 주석 처리된 상태입니다. 릴리즈 빌드에서는 반드시 주석 처리하거나 삭제하세요.

    WARNING: Remove this option for release binaries!
    Options for in-app testing on your global store (Available from IAP v21.04.00)
     - onestore_00 : South Korea
     - onestore_01 : Singapore, Taiwan
     - onestore_02 : United States (Digital Turbine)
     - onestore_03 : ONE Billing Lab

    If not set, defaults to South Korea.
-->
<!--<meta-data android:name="onestore:dev_option" android:value="onestore_03" />-->
