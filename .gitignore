# === 이 저장소에 들어가면 안 되는 것 =========================================
# 에이전트 작업 규칙 파일 — 로컬 경로·작업 맥락이 담기므로 올리지 않는다
CLAUDE.md
AGENTS.md
GEMINI.md
.claude/
.agents/
.cursor/
.codex/
.gemini/
.grok/
.junie/
.github/skills/

# 설치 산출물 — 설치할 때 대상 프로젝트에 생기는 것이다
.install-manifest

# 자격증명·키 — 어떤 형태로도 올리지 않는다
*.jks
*.keystore
keystore.properties
*.pem
*.p8
*.p12
.env
.env.*
local.properties
secrets.properties

# === 일반 ====================================================================
# macOS
.DS_Store
.AppleDouble
._*

# 에디터·IDE
.idea/
.vscode/
*.swp
*~

# 로컬 실험·임시 산출물
/scratch/
/tmp/
*.log
*.tmp
*.orig
*.rej

# 안드로이드·Gradle (테스트용 프로젝트를 이 안에 두는 경우)
build/
.gradle/
captures/
